# Terraform Redshift Setup

## Running terraform setup

* Install [terraform](https://www.terraform.io/)

* `terraform init -var-file="dev.tfvars"`
* `terraform plan -var-file="dev.tfvars"`
* `terraform apply -var-file="dev.tfvars"`